# MatMan — 안전재고(Safe_tb) 설계 문서

> 이 문서는 **설계 의도**와 **실제 데이터 검증 결과**를 함께 담는다.
> 각 항목의 ✅ 표시는 `data/erp.db` 실측으로 확인된 것, ⚠️는 설계와 데이터가 다른 부분이다.

**핵심 설계 사상**: 안전재고를 담당자의 감으로 정하지 않는다.
**5개 항목 점수제로 등급을 매기고 → 공식으로 수량을 계산해 `Sf_Num`에 반영하고 → 등급별 주기마다 재계산해 갱신한다.**

---

## 1. 테이블 구조

```
Safe_tb
────────────────────────────────────────────────────────────
컬럼명         타입          설명
────────────────────────────────────────────────────────────
P_ID          VARCHAR(9)   자재품번 (PK, Product_tb FK)
Lead_Time     Long         리드타임(일) — 발주일→입고일 평균
Sf_Lv         VARCHAR(1)   재고등급 (A / B / C)
Sf_Num        Long         안전재고 수량 (공식 계산값)
Price_Score   Long         단가 점수         (1~3점)
Sub_Score     Long         대체가능성 점수    (1~3점)
Impact_Score  Long         품절 시 영향 점수  (1~3점)
Supply_Score  Long         공급 안정성 점수   (1~3점)
Usage_Score   Long         연간사용금액 점수  (1~3점, 신규 등록 시 NULL)
────────────────────────────────────────────────────────────
```

현재 데이터: **200종** (신규 상태 70종 / 갱신 완료 130종)

---

## 2. ABC 등급 기준 — 5개 항목 점수제

각 항목을 **A=3점 / B=2점 / C=1점**으로 채점 후 합산한다.

### 항목별 채점 기준

| 항목 | 3점 (A) | 2점 (B) | 1점 (C) |
|---|---|---|---|
| **Price_Score**<br>단가 | 10만원 이상 | 5천원 ~ 10만원 미만 | 5천원 미만 |
| **Sub_Score**<br>대체 가능성 | 대체 불가 / 어려움 | 일부 대체 가능 | 쉽게 대체 가능 |
| **Impact_Score**<br>품절 시 영향 | 생산 중단 / 핵심 차질 | 일부 지연 | 영향 미미 |
| **Supply_Score**<br>공급 안정성 | 공급처 1곳 / 해외 소싱 | 공급처 2~3곳 | 다수 공급처 / 국내 조달 |
| **Usage_Score**<br>연간 사용금액 | 상위 70~80% | 중간 15~20% | 하위 5~10% |

`Usage_Score`는 사람이 매기지 않는다. **파레토 분석으로 자동 산정**한다(6장 참조).
신규 등록 시점에는 사용 이력이 없으므로 **NULL**로 두고, 데이터가 쌓인 뒤 갱신 시 채운다.

### 등급 컷오프 — 신규와 갱신이 다르다 (중요)

```
① 신규 등록  (Usage_Score 없음 → 4개 항목, 만점 12점)
     A등급 : 10점 이상
     B등급 :  6 ~ 9점
     C등급 :  5점 이하

② 갱신 이후  (5개 항목 전체, 만점 15점)
     A등급 : 13점 이상
     B등급 :  8 ~ 12점
     C등급 :  7점 이하
```

> ✅ **실측 검증 완료 — 예외 0건**
> | 대상 | 적용 컷오프 | 결과 |
> |---|---|---|
> | 신규 70종 (`Usage_Score IS NULL`) | 4항목 10/6/5 | **불일치 0건 / 70건** |
> | 갱신 130종 (`Usage_Score` 있음) | 5항목 13/8/7 | **불일치 0건 / 130건** |
>
> 신규 70종의 4항목 합계 분포: 5점→C 3건 / 6점→B 10건 / 7점→B 26건 / 8점→B 14건 / 9점→B 15건 / 10점→A 2건
>
> **이 이중 컷오프 구조가 데이터에 한 건의 예외 없이 구현되어 있음이 확인되었다.**
> `Usage_Score`가 비어 있는 70건은 데이터 결함이 아니라 **"아직 갱신 주기가 도래하지 않은 신규 등록 상태"** 다.

---

## 3. 안전재고 수량 계산 공식

```
SS = Z × √( L×σ_d² + d²×σ_L² )

ROP (재주문점) = d × L + SS
```

### 파라미터

| 기호 | 의미 | 산출 방법 |
|---|---|---|
| **Z** | 등급별 서비스 수준 계수 | A = 2.33 (99%)<br>B = 1.65 (95%)<br>C = 1.28 (90%) |
| **L** | 평균 리드타임(일) | `Purchase_Header_tb.P_Date` → `Lot_tb.Lot_Date` 차이의 평균 |
| **d** | 일평균 수요량 | `Transaction_tb` 출고 합계 ÷ 운영일수(184일) |
| **σ_d** | 수요 표준편차 | 초기 추정치: A = d×0.20 / B = d×0.25 / C = d×0.30 |
| **σ_L** | 리드타임 표준편차 | 초기 추정치: A = L×0.20 / B = L×0.25 / C = L×0.30 |

### 초기 데이터가 없을 때
```
σ_d = d × 0.25 고정값으로 시작
→ 데이터 10건 이상 확보 시 실측값으로 전환
```

> ✅ **L(리드타임) 실측 가능 확인**
> `Lot_tb` ⋈ `Purchase_Header_tb` 로 **발주-입고 쌍 636건** 확보.
> 전체 평균 **12.1일** (범위 2~70일). 품목별 평균을 실제로 계산할 수 있다.

> ⚠️ **`Sf_Num`은 현재 결과값만 존재하고 계산 근거(d, σ_d, σ_L, Z)가 DB에 없다.**
> 화면에 "이 안전재고가 왜 이 숫자인가"를 보이려면 위 표대로 `Transaction_tb`에서
> d와 σ_d를 계산해 함께 제시해야 한다. → 구현 시 우선 과제.

---

## 4. 신규 등록 vs 갱신 — 운영 구조

```
■ 신규 자재 등록 시
   → 4개 항목(Price/Sub/Impact/Supply) 체크리스트 입력
   → 4개 항목 합산으로 등급 결정 (컷오프 10 / 6 / 5점)
   → Usage_Score = NULL
   → Sf_Num = 공식 계산 (초기 추정 σ 사용)
   → Update_Log_tb 에 등록 이력 기록

■ 갱신 주기 도달 시 (Update_Log_tb.Next_Date 기준)
   갱신 주기:  A등급 30일 / B등급 90일 / C등급 180일
              ※ 2026-09-10 변경. 고가·핵심(A)일수록 자주 검토하는 ABC 원칙 적용.
                원본은 A=180/B=90/C=30 으로 정반대였음.

   → Transaction_tb 출고 집계
        출고수량 × 단가 = 연간 사용금액
        파레토 누적비율 → Usage_Score A/B/C 자동 산정
   → 5개 항목 전체로 등급 재산정 (컷오프 13 / 8 / 7점)
   → Sf_Num 재계산 (실측 σ 사용)
   → Update_Log_tb 갱신, Audit_Log_tb 자동 기록
```

> ✅ **갱신 주기 — 재계산 적용 완료 (예외 0건)**
> `Update_Log_tb`의 `Updated_Date` → `Next_Date` 간격:
> **A등급 21종 전부 30일 · B등급 160종 전부 90일 · C등급 19종 전부 180일**
>
> ⚠️ 원본은 A=180 / B=90 / C=30 이었다. ABC 재고관리 원칙(중요 자재일수록 자주 검토)과
> 정반대라 6차 회의(7/01) 결정안으로 되돌렸다. `build_db.py` → `REVIEW_CYCLE_DAYS`.

> ✅ **신규 등록 시에도 `Update_Log_tb`에 기록된다** — 신규 70종 전부(70/70) 이력 보유. 설계와 일치.

---

## 5. 연관 테이블

```
Update_Log_tb   P_ID(PK), Updated_Date(PK), Next_Date
  → 안전재고 갱신 이력 및 다음 갱신 예정일 관리
  ✅ 실제 존재. 200행.

Audit_Log_tb    Log_ID, Target_Table, Target_ID,
                Action, Reason, Modified_By, Modified_At
  → 등급 변경, 수동 조정 시 자동 기록
  ⚠️ 설계상으로만 존재. 원본 데이터(accdb/CSV)에 테이블이 없음.
     구현하려면 신규 생성해야 함.
```

---

## 6. Usage_Score 계산 로직 (파레토 분석)

```python
# Transaction_tb + Lot_tb + Product_tb 연계
usage_qty = {}  # P_ID → 총 출고수량
for tx in transactions:
    if tx['T_Type'] == '불출':          # ⚠️ 실제 데이터는 '출고'가 아니라 '불출'
        pid = lot_pid[tx['Lot_ID']]
        usage_qty[pid] = usage_qty.get(pid, 0) + int(tx['T_Num'])

# 연간 사용금액 = 출고수량 × 단가
usage_amount = {pid: qty * price[pid] for pid, qty in usage_qty.items()}

# 파레토 누적비율로 A/B/C 분류
total = sum(usage_amount.values())
cumul = 0
for pid, amt in sorted(usage_amount.items(), key=lambda x: x[1], reverse=True):
    cumul += amt
    ratio = cumul / total * 100
    if ratio <= 75:
        usage_grade[pid] = 'A'   # → 3점
    elif ratio <= 90:
        usage_grade[pid] = 'B'   # → 2점
    else:
        usage_grade[pid] = 'C'   # → 1점
```

---

## 7. 구현 시 주의 — 설계문서와 실제 데이터의 차이

| # | 항목 | 설계문서 | 실제 데이터 | 대응 |
|---|---|---|---|---|
| 1 | **출고 구분값** | `T_Type == '출고'` | **`'불출'`** (439건) | 코드에서 `'불출'`로 비교해야 함. `'출고'`로 짜면 결과가 0이 나옴 |
| 2 | **운영일수** | 184일 고정 | 거래 전체 **219일**<br>(2025-07-03 ~ 2026-02-07)<br>불출 건만 **208일** | 184일은 2025-07-01~12-31(6개월) 기준으로 보임. d 계산 시 어느 기준을 쓸지 확정 필요 |
| 3 | **Audit_Log_tb** | 있음 | **없음** | 필요 시 신규 생성 |
| 4 | **Sf_Num 근거** | 공식으로 산출 | 결과값만 존재 | d, σ_d를 `Transaction_tb`에서 계산해 화면에 근거 제시 |
| 5 | **Safe_tb 원본** | — | accdb는 `Sf_Num` 200/201 NULL | **`Safe_tb_v2.csv`가 정본** (200건 전부 채워짐) |

---

## 8. 검증 요약

| 검증 항목 | 대상 | 결과 |
|---|---|---|
| 신규 등록 4항목 컷오프 (10/6/5) | 70종 | ✅ 불일치 **0건** |
| 갱신 후 5항목 컷오프 (13/8/7) | 130종 | ✅ 불일치 **0건** |
| 등급별 재검토 주기 (30/90/180일) | 200종 | ✅ 예외 **0건** (2026-09-10 역전 적용) |
| 신규 등록 시 이력 기록 | 70종 | ✅ **70/70** 기록됨 |
| 리드타임 실측 가능 여부 | 636쌍 | ✅ 평균 12.1일 산출 가능 |

**→ 설계가 데이터까지 예외 없이 관철되어 있다.**
현 시점 안전재고 미달 품목: **67종 / 200종 (33.5%)**
